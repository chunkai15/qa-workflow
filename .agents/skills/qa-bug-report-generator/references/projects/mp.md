# MP Project Notes

Use this file after the target project is confirmed as `MP`.

## Current State

This profile is scaffolded but intentionally incomplete.

Pending confirmation:

- `QA` field id
- sprint field id
- default QA user
- Slack assignee mappings
- reusable story-routing heuristics

## Interim Rule

For `MP`, prefer:

1. explicit user instruction
2. feature snapshot artifact in the repo
3. live Jira lookup

Only use project defaults here after they are explicitly confirmed and added to `mp.yaml`.
