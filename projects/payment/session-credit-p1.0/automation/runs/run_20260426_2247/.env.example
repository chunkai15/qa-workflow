# Everfit Staging Credentials
# Copy this to .env in the run directory before running tests manually

BASIC_AUTH_USER=everfit
BASIC_AUTH_PASS=<set-at-runtime>

# Coach Login
TEST_EMAIL=<set-at-runtime>
TEST_PASSWORD=<set-at-runtime>
